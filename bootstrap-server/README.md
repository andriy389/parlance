# Parlance Bootstrap Server

A lightweight WebSocket server for internet-scale peer discovery in the Parlance P2P messaging platform.

## Overview

The bootstrap server acts as a central signaling point that helps Parlance peers discover each other across the internet. It maintains a registry of online peers and their addresses, enabling clients to find and connect to each other even when they're on different networks.